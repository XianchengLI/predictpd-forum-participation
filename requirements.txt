# Python 3.9 or later. Versions below are the ones the reproduction in REPRODUCTION.md was run with;
# earlier versions of the same major release are expected to give identical results.
pandas>=2.0
numpy>=1.24
scipy>=1.10
statsmodels>=0.14
networkx>=3.0
openpyxl>=3.1
